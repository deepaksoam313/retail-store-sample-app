{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "carts.serviceAccountName" . }}
  labels:
    {{- include "carts.labels" . | nindent 4 }}
  annotations:
    {{- with .Values.serviceAccount.annotations }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
    {{- if .Values.secrets.irsaRoleArn }}
    eks.amazonaws.com/role-arn: {{ .Values.secrets.irsaRoleArn }}
    {{- end }}
{{- end }}
